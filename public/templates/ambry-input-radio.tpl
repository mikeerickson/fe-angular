<div>
  <label class="{{vm.class}}" for="{{vm.name}}">
    <input
      type=     "radio"
      name=     "{{vm.name}}"
      class=    "{{vm.class}}"
      value=    "{{vm.value}}"
      ng-model= "vm.model" /> {{vm.label}}
  </label>
</div>
