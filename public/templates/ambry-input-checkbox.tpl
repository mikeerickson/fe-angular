<div>
  <label class="{{vm.class}}" for="{{vm.name}}">
    <input
      type=     "checkbox"
      name=     "{{vm.name}}"
      id=       "{{vm.name}}"
      class=    "{{vm.class}}"
      ng-model= "vm.model" /> {{vm.label}}
  </label>
</div>
