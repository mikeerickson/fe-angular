<div>
  <label for="{{vm.name}}">{{vm.label}}</label>
  <input
    class=    "form-control {{vm.class}}"
    type=     "text"
    name=     "{{vm.name}}"
    id=       "{{vm.name}}"
    ng-model= "vm.model"
    />
</div>

